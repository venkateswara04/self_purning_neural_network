# self_purning_neural_network
This project develops a self-pruning neural network that learns to remove unnecessary weights during training using learnable gate parameters. An L1-based sparsity loss encourages the network to retain only important connections. The model is evaluated on the CIFAR-10 dataset to study the trade-off between accuracy and sparsity
